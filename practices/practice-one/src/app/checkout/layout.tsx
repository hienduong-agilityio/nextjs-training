export const metadata = {
  title: 'E-comm - Checkout',
};

// Layout Component
export default function CheckoutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
